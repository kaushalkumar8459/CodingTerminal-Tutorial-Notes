# Real Interview Set 6 — 100+ Coding Question Bank

This bank collects practical JavaScript interview questions from the supplied notes. Write your own solution for each question.

## Arrays and Algorithms

1. Reverse an array without mutating the original.

2. Find the maximum and minimum values without sorting.

3. Calculate the sum and average of an array.

4. Remove duplicate primitive values while preserving order.

5. Implement a numeric sorting algorithm without `.sort()`.

6. Find the union and intersection of two arrays.

7. Rotate an array right by `k` positions.

8. Find the largest contiguous subarray sum.

9. Check whether an array is a palindrome.

10. Shuffle an array fairly with Fisher-Yates.

11. Convert an array of records into an object keyed by `name`.

12. Find duplicate values and their counts.

13. Remove empty strings from an array.

14. Find a pair with a target sum in O(n).

15. Rotate an array by the first value and return the result as a string.

16. Merge two sorted arrays without sorting the combined array.

17. Find the missing number from `0..n`.

18. Find all missing numbers in a sorted positive array.

19. Find the first duplicate value.

20. Move all zeroes to the end while preserving order.

21. Find the second largest distinct number.

22. Find the kth smallest and kth largest values.

23. Split an array into chunks of a requested size.

24. Flatten a multidimensional array without `.flat()`.

25. Sum all values in a jagged nested array.

26. Print a matrix in spiral order.

27. Find the largest value in every row of a matrix.

28. Find all triplets that sum to a target.

29. Find the closest number to a target.

30. Generate every subset of an array.

## Strings

31. Reverse a string without using `.reverse()`.

32. Reverse word order without reversing letters.

33. Reverse letters inside each word while preserving word order.

34. Check whether a string is a palindrome.

35. Check whether two strings are anagrams.

36. Compress consecutive characters, such as `aaaabbc` to `a4b2c1`.

37. Capitalize the first letter of every word.

38. Find the longest word in a sentence.

39. Find the longest common prefix of an array of strings.

40. Determine whether one string is a rotation of another.

41. Generate all permutations of a string.

42. Truncate text and append an ellipsis.

43. Validate alphanumeric characters.

44. Count substring occurrences.

45. Count vowels and consonants.

46. Find the first non-repeating character.

47. Find the most frequent character.

48. Remove duplicate characters while preserving first occurrence.

49. Replace every digit in a phone number except the first and last two digits with `*`.

50. Convert `29/12/2022` to `2022-12-29`.

51. Remove all whitespace from a string.

52. Replace every occurrence of a substring.

53. Find the longest substring containing K unique characters.

54. Insert `-` between adjacent odd digits and `*` between adjacent even digits.

55. Group a list of words into anagram groups.

## Objects and Data Transformation

56. Merge two objects with the second object winning conflicts.

57. Deep-clone a nested object.

58. Serialize and deserialize an object with JSON.

59. Compare two objects deeply.

60. Transform records into an object keyed by ID.

61. Filter an object by value or key.

62. Sort object records by numeric and string properties.

63. Count the number of keys in an object.

64. Add, update, and remove object properties safely.

65. Check whether a key is an own property.

66. Sum matching keys across an array of objects.

67. Group records by age, category, or department.

68. Build a student result system with totals, averages, and grades.

69. Implement a class with inheritance.

70. Simulate private object data with closures.

71. Implement getters and setters with validation.

72. Convert dot notation such as `a.b.c` into a nested object.

73. Convert a nested object into dot-notation keys.

74. Explain shallow versus deep copy with nested data.

75. Use `Object.freeze`, `Object.seal`, and property descriptors.

## JavaScript language and output questions

76. Explain `typeof null`, `typeof NaN`, and `typeof []`.
77. Predict `true + false`, `"2" + true`, and `-"34" + 10`.
78. Explain `null == undefined` versus strict equality.
79. Predict `var` hoisting and the temporal dead zone for `let` and `const`.
80. Explain why function declarations and function expressions hoist differently.
81. Predict a `var` loop with `setTimeout`.
82. Predict the same loop using `let`.
83. Explain array and object reference equality.
84. Predict spread with nested objects.
85. Explain `delete` on an object property versus a variable.
86. Predict Promise microtasks versus timers.
87. Explain why `await` pauses one function but not the event loop.
88. Explain `call`, `apply`, and `bind`.
89. Explain arrow-function `this` versus regular-function `this`.
90. Explain event bubbling, capturing, and `stopPropagation`.

## Polyfills, functions, and practical browser tasks

91. Implement `forEach`, `map`, `filter`, and `reduce` polyfills.
92. Implement a `bind` polyfill.
93. Implement `Promise.all` and `Promise.allSettled` behavior.
94. Implement currying for unlimited arguments.
95. Implement memoization with a cache.
96. Implement debounce and throttle.
97. Build an event-delegated counter or todo list.
98. Build form validation using regex and `FormData`.
99. Build a retrying fetch wrapper with timeout and cancellation.
100.  Build a small API dashboard with search, filter, sort, pagination, and Local Storage.
101.  Implement a stack and queue.
102.  Detect a linked-list cycle.
103.  Implement BFS and DFS.
104.  Implement binary search.
105.  Implement merge sort.

## Interview answer standard

For every question, explain the edge cases, time complexity, space complexity, mutation behavior, and why your chosen approach is suitable.
