# Solutions — Day 023: Performance, Memory & Optimization

These are solution-design guides. The candidate should write the implementation and defend the trade-offs.

1. **Find O(n²) bottleneck** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

2. **Optimize repeated searches** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

3. **Set versus includes** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

4. **Optimize frequency counting** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

5. **Optimize nested lookup** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

6. **Avoid shared-state mutation** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

7. **Detect retaining closure** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

8. **Bounded cache** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

9. **Cache eviction** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

10. **Measure complexity** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

11. **Recursion versus iteration** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

12. **Optimize string processing** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

13. **Large object transformation** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

14. **Chunk large arrays** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

15. **Chunked computation** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

16. **Idle-time computation** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

17. **Avoid main-thread blocking** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

18. **Worker-friendly design** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

19. **Memoize expensive results** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

20. **Detect duplicate network work** — Start by clarifying constraints and correctness requirements. Give a simple baseline, identify its bottleneck, then select the appropriate data structure, algorithm, async primitive, or architecture. Explain invariants, failure handling, complexity, and tests.

## Senior review

A strong solution should discuss maintainability, observability, cancellation/error handling where relevant, memory growth, concurrency, and how the design behaves at scale.