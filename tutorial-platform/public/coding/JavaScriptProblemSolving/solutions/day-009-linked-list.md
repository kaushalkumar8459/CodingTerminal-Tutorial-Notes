# Solutions — Day 009: Linked Lists

These are solution approaches rather than copy-paste answers. Implement them in JavaScript and verify with tests.

1. **Create a singly linked list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

2. **Insert at head** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

3. **Insert at tail** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

4. **Insert at an index** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

5. **Delete by value** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

6. **Delete at an index** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

7. **Find list length** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

8. **Search a linked list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

9. **Reverse a linked list iteratively** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

10. **Reverse a linked list recursively** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

11. **Find the middle node** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

12. **Detect a cycle** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

13. **Find cycle entry** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

14. **Remove a cycle** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

15. **Merge two sorted lists** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

16. **Merge K sorted lists** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

17. **Remove Nth node from end** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

18. **Remove duplicates from sorted list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

19. **Remove duplicates from unsorted list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

20. **Check palindrome list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

21. **Find intersection of two lists** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

22. **Add two numbers represented by lists** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

23. **Rotate a linked list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

24. **Partition a list around a value** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

25. **Reverse nodes in groups of K** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

26. **Swap nodes in pairs** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

27. **Copy list with random pointers** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

28. **Flatten a multilevel linked list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

29. **Sort a linked list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

30. **Reorder a linked list** — Use the standard linked list technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

## Review checklist

- Correctness on empty, single-item, duplicate, boundary, and large inputs.
- Time complexity and auxiliary space.
- Mutation versus non-mutating behavior.
- Why the chosen data structure fits the problem.
- How the solution changes under tighter memory or latency constraints.
