# Solutions — Day 018: Advanced JavaScript Problem Solving

Use this as a solution guide and implement/test each challenge yourself.

1. **bind polyfill** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

2. **call polyfill** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

3. **apply polyfill** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

4. **map polyfill** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

5. **filter polyfill** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

6. **reduce polyfill** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

7. **flat polyfill** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

8. **once** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

9. **debounce** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

10. **throttle** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

11. **memoization** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

12. **currying** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

13. **partial application** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

14. **compose** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

15. **pipe** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

16. **EventEmitter** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

17. **Promise.all** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

18. **Promise.race** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

19. **Promise.allSettled** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

20. **Promise.any** — Choose the data structure or algorithm that matches the constraint. Establish the invariant, handle empty/boundary input, implement the baseline approach, then improve time or space when justified. Test duplicates, invalid input, and large input.

## Interview review
State Big-O, auxiliary space, mutation behavior, and at least one alternative approach.