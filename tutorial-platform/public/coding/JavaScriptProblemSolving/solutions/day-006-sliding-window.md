# Solutions — Day 006: Sliding Window & Range Techniques

These are solution approaches rather than copy-paste answers. Implement them in JavaScript and verify with tests.

1. **Maximum sum of K consecutive values** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

2. **Minimum sum of K consecutive values** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

3. **Average of every K-size window** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

4. **Maximum value in every window** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

5. **Longest substring without repeating characters** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

6. **Longest substring with at most K distinct characters** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

7. **Longest substring with exactly K distinct characters** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

8. **Minimum window containing a pattern** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

9. **Find all anagram windows** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

10. **Longest subarray with at most K zeroes** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

11. **Longest subarray after replacing K values** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

12. **Count subarrays with at most K distinct values** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

13. **Count subarrays with exactly K distinct values** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

14. **Smallest subarray with sum at least target** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

15. **Longest subarray with sum at most target for positive values** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

16. **Fruit into baskets** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

17. **Max consecutive ones with one replacement** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

18. **Max consecutive ones with K replacements** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

19. **Permutation in string** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

20. **Longest repeating character replacement** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

21. **Subarrays with product less than K** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

22. **Binary subarrays with sum K** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

23. **Number of nice subarrays** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

24. **Maximum points from cards** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

25. **Sliding-window rate limiter** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

26. **Rolling average calculator** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

27. **Rolling event counter** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

28. **Detect traffic spikes using a window** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

29. **Windowed unique-user count** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

30. **Windowed error-rate calculation** — Use the standard sliding window technique appropriate to the problem. State the invariant, handle empty/boundary input, and target the best practical complexity. When multiple approaches exist, compare brute force with the optimized approach.

## Review checklist

- Correctness on empty, single-item, duplicate, boundary, and large inputs.
- Time complexity and auxiliary space.
- Mutation versus non-mutating behavior.
- Why the chosen data structure fits the problem.
- How the solution changes under tighter memory or latency constraints.
